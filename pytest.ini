[pytest]
# Only collect the real automated suite. The legacy ad-hoc API-ping scripts live
# under tests/manual/ (renamed off the test_ prefix) and must never run in CI —
# they make live network calls on import.
testpaths = tests/unit tests/integration
python_files = test_*.py
python_functions = test_*
addopts = -ra
